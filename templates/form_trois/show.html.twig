{% extends 'base.html.twig' %}

{% block title %}FormTrois{% endblock %}

{% block body %}
    <h1>FormTrois</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_troi.id }}</td>
            </tr>
            <tr>
                <th>Loisir</th>
                <td>{{ form_troi.loisir }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('form_trois_index') }}">back to list</a>

    <a href="{{ path('form_trois_edit', {'id': form_troi.id}) }}">edit</a>

    {{ include('form_trois/_delete_form.html.twig') }}
{% endblock %}
