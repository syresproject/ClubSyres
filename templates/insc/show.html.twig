{% extends 'base.html.twig' %}

{% block title %}Insc{% endblock %}

{% block body %}
    <h1>Insc</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ insc.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ insc.name }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ insc.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('insc_index') }}">back to list</a>

    <a href="{{ path('insc_edit', {'id': insc.id}) }}">edit</a>

    {{ include('insc/_delete_form.html.twig') }}
{% endblock %}
